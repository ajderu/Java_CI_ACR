apiVersion: v1
kind: Service
metadata:
    name: adrianj-go
    namespace: adrianj-go
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 8080
      protocol: TCP 
    selector:
        app: adrianj-go
